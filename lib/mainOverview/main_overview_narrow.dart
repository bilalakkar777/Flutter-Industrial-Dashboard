import 'package:flutter/material.dart';

class MainOverviewNarrowLayout extends StatelessWidget {
  const MainOverviewNarrowLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Placeholder(
      strokeWidth: 0,
      child: OutlinedButton(
        child: Text("BLA"),
        onPressed: () => {},
      ),
    ));
  }
}
